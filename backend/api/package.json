{
  "name": "@aether/api-meta",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "python -m compileall src",
    "lint": "ruff check src",
    "typecheck": "mypy src"
  }
}
